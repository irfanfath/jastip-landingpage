version: '3.4'
networks:
  jastip-app:
    driver: bridge

services:
  #SERVICE
  landing-page:
    container_name: landing-page
    hostname: "landing-page"
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8000:80"
    networks:
      - jastip-app